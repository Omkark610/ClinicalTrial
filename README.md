Steps to run project:
1) create .env file by copying .env.example file
2) run "composer install" command in omkar-test folder
3) run "php artisan migrate" command
4) run "php artisan serve"command and visit http://localhost:8000/